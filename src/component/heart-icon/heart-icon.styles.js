import { makeStyles } from "@material-ui/core/styles";

const HeartIconStyles = makeStyles({
  icon: {
    fontSize: "2.5rem",
    fill: "#d3d3d3",
    fillOpacity: ".5",
    stroke: "white",
    strokeWidth: ".4",
    transition: "all .2s ease-in",
  },
});

export default HeartIconStyles;
