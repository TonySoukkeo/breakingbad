import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  card: {
    position: "relative",
    minWidth: 275,
  },
  media: {
    height: 560,
  },
});

export default useStyles;
