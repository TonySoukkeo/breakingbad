import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  "mt-sm": {
    marginTop: "1rem",
  },
  "mb-sm": {
    marginBottom: "1rem",
  },
  "text-bold": {
    fontWeight: "bold",
  },
  link: {
    textDecoration: "none",
  },
});

export default useStyles;
